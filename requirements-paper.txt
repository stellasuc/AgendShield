# Isolated runtime for the pinned AWM + BrowserGym + WebArena path.
# This mirrors third_party/agent-workflow-memory/webarena/requirements.txt while
# separating an upstream line-break typo (`httpx==0.27.2gymnasium`) into two
# valid requirements. The upstream submodule remains unmodified.
browsergym-core>=0.3
browsergym-experiments>=0.3
browsergym-webarena
openai>=1.35.4,<1.36
langchain>=0.2,<0.3
langchain_openai>=0.1.10,<0.2
tiktoken
huggingface_hub
contexttimer
ipython
pyyaml>=6,<7
pandas
joblib
transformers
langchain_community>=0.2.6,<0.3
httpx==0.27.2
gymnasium
